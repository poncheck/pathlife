{
  "name": "pathlife",
  "version": "1.0.0",
  "description": "Personal photo diary with Immich, Strava and Traccar integration",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\"",
    "build": "npm run build -w apps/backend && npm run build -w apps/frontend",
    "start": "npm run start -w apps/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
